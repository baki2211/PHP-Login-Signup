{% extends 'base.html.twig' %}

{% block title %}Famaton Registeration{% endblock %}

{% block body %}

<div class="container">
    <div class="center text-center p-5 mb-2">
        <a href="{{ path('app_home') }}"><img src="{{ asset('img/logo.png') }}" alt="logo"></a>
    </div>
</div>
<div class="container">
    <div class="center">
        <div class="row row-cols-3">
            <div class="col"></div>
            <div class="col">
                <div class="center card logincard p-3 ">
            <div class="card-body p-3" >
            <div class="card-title">
                <h3>Create Account</h3>
            </div>
                {{ form_errors(registrationForm) }}
                {{ form_start(registrationForm) }}
                {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}
        <div class="row d-flex m-2"> <button type="submit" class=" btn btn-lg btn-warning">Register</button></div>
       
    {{ form_end(registrationForm) }}
            </div>
        </div>
        <div class="text-center m-2">
            <p>Already have an account? <a class="" href="{{ path('app_login') }}">Sign in</a></p>
            
        </div>
        
    </div>
            <div class="col"></div>
        </div>
        
    </div>
</div>

{% endblock %}